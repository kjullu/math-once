#import "../math-once.typ": calculate, qalc, qalc-builder

#assert(type(calculate) == function)
#assert(type(qalc) == function)
#assert(type(qalc-builder) == function)

The documented public API exports all three functions.
