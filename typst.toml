[package]
name = "math-once"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Mikkel"]
license = "MIT"
description = "Qalc-like calculations with reusable values, physical units, and automatic conversion."
categories = ["utility"]
compiler = "0.14.0"
